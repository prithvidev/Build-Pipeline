pipeline{
  agent none
  stages{
    stage('Checking if sheel commands are working or not'){
      docker { image 'ubuntu' }
      steps{
        sh '''
        pwd
        sh '''
      }
    }
  }
}
